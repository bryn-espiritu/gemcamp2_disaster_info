<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <header>
    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #f3fde3;">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Disaster-Info</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Home", posts_path, class: "nav-link active" %>
            </li>
            <li class="nav-item">
              <%= link_to "Disaster", categories_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Sign in", new_user_session_path, class: 'btn btn-light' unless user_signed_in? %>
              <%= button_to 'Sign-Out', destroy_user_session_path, class: 'btn btn-light', method: :delete if user_signed_in? %>
            </li>
            <li class="nav-item">
              <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true"><p><%= "Welcome #{current_user.email}" if current_user %></p></a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <body>

  <p class="notice" style="color: #4ac136"><%= notice %></p>
  <p class="alert" style="color: #c10709"><strong><%= alert %></strong></p>

  <%= yield %>
  </body>
</html>
